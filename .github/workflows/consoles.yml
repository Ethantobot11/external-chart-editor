name: Build Consoles

on:
  workflow_call:
    inputs:
      name:
        required: true
        type: string
      os:
        required: true
        type: string
      buildArgs:
        required: true
        type: string
      artifactName:
        required: true
        type: string
      artifactPath:
        required: true
        type: string

env:
  HAXE_VERSION: 4.3.6

jobs:
  build-console:
    name: Build
    runs-on: ${{ inputs.os }}
    container:
      image: devkitpro/toolchain-base:latest
    steps:
      - name: Checkout
        uses: actions/checkout@main

      - name: Setup Haxe
        uses: krdlab/setup-haxe@master
        with:
          haxe-version: ${{ env.HAXE_VERSION }}

      - name: Install System Dependencies (Wii U)
        if: inputs.name == 'Wii U'
        run: |
          apt-get update && apt-get install -y build-essential git wget curl

      - name: Install Libraries
        run: |
          haxelib install hmm --quiet
          haxelib run hmm install --quiet

      - name: Install Console Toolchains
        run: |
          if [ "${{ inputs.name }}" = "Nintendo 3DS" ]; then
            dkp-pacman -S --noconfirm 3ds-dev citro3d citro2d
          elif [ "${{ inputs.name }}" = "Wii U" ]; then
            dkp-pacman -S --noconfirm ppc-dev wut-dev wuhbtool
          fi

      - name: Compile for 3DS via Haxe3DS
        if: inputs.name == 'Nintendo 3DS'
        run: haxelib run haxe3ds -c

      - name: Compile for Wii U via HxCompileU
        if: inputs.name == 'Wii U'
        run: |
          git clone https://github.com/Slushi-Github/hxCompileU.git
          cd hxCompileU
          haxe build.hxml
          cp export/haxeCompileU ../hxCompileU-bin
          cd ..
          ./hxCompileU-bin --prepare
          ./hxCompileU-bin --compile

      - name: Upload Artifact
        uses: actions/upload-artifact@main
        with:
          name: ${{ inputs.artifactName }}
          path: ${{ inputs.artifactPath }}
          if-no-files-found: error
