name: Build Consoles

on:
  workflow_call:
    inputs:
      name:
        required: true
        type: string
      os:
        required: true
        type: string
      buildArgs:
        required: true
        type: string
      artifactName:
        required: true
        type: string
      artifactPath:
        required: true
        type: string

env:
  HAXE_VERSION: 4.3.6

jobs:
  build-console:
    name: Build
    runs-on: ${{ inputs.os }}
    container:
      image: ${{ inputs.name == 'Nintendo 3DS' && 'devkitpro/devkitarm:latest' || 'devkitpro/devkitppc:latest' }}
    steps:
      - name: Checkout
        uses: actions/checkout@main

      - name: Setup Haxe
        uses: krdlab/setup-haxe@master
        with:
          haxe-version: ${{ env.HAXE_VERSION }}

      - name: Install System Dependencies (Wii U)
        if: inputs.name == 'Wii U'
        run: |
          apt-get update && apt-get install -y build-essential git wget curl

      - name: Install Libraries & Fix Hxcpp (For 3DS)
        run: |
          haxelib install hmm --quiet
          haxelib run hmm install --quiet
          
          if [ "${{ inputs.name }}" = "Nintendo 3DS" ]; then
            haxelib remove hxcpp
            haxelib git hxcpp https://github.com/PsychEngine-report/hxcpp --quiet --always
            haxelib remove flixel
            haxelib git flixel https://github.com/Ethantobot11/flixel-3DS --quiet --always
            haxelib git haxe3ds https://github.com/Ethantobot11/Haxe3DS.git --quiet --always
            haxelib git reflaxe https://github.com/SomeRanDev/reflaxe --quiet
            haxelib git reflaxe.cpp https://github.com/SomeRanDev/reflaxe.CPP --quiet
          fi

      - name: Install Console Toolchains
        run: |
          if [ "${{ inputs.name }}" = "Nintendo 3DS" ]; then
            dkp-pacman -S --noconfirm --needed 3ds-dev citro3d citro2d
          elif [ "${{ inputs.name }}" = "Wii U" ]; then
            dkp-pacman -S --noconfirm --needed devkitppc-gcc wut wut-tools general-tools
          fi

      - name: Build Hxcpp Tooling (For 3DS)
        if: inputs.name == 'Nintendo 3DS'
        run: |
          cd .haxelib/hxcpp/git/tools/hxcpp
          haxe compile.hxml
          cd ../../../../

      - name: Create Missing 3DS Romfs Directory
        if: inputs.name == 'Nintendo 3DS'
        run: |
          mkdir -p assets/romfs/haxe3ds
          touch assets/romfs/haxe3ds/version

      - name: Force Cross-Compiler in Haxe3DS Toolchain
        if: inputs.name == 'Nintendo 3DS'
        run: |
          TOOLCHAIN_XML=$(find .haxelib/hxcpp -name "linux-toolchain.xml" -o -name "haxe3ds-setup.xml" | head -n 1)
          if [ -f "$TOOLCHAIN_XML" ]; then
            echo "Patching toolchain file: $TOOLCHAIN_XML"
            sed -i 's/<compiler id="gcc" name="g++"/<compiler id="gcc" name="arm-none-eabi-g++"/g' "$TOOLCHAIN_XML"
            sed -i 's/<compiler id="linker" name="g++"/<compiler id="linker" name="arm-none-eabi-g++"/g' "$TOOLCHAIN_XML"
          fi
          
          echo "CXX=arm-none-eabi-g++" >> $GITHUB_ENV
          echo "CC=arm-none-eabi-gcc" >> $GITHUB_ENV
          echo "AR=arm-none-eabi-ar" >> $GITHUB_ENV

      - name: Compile for 3DS via Haxe3DS
        if: inputs.name == 'Nintendo 3DS'
        run: |
              export CXX=arm-none-eabi-g++
              export CC=arm-none-eabi-gcc
              haxelib run haxe3ds -c -Dxcompile

      - name: Compile for Wii U via HxCompileU
        if: inputs.name == 'Wii U'
        run: |
          git clone https://github.com/Slushi-Github/hxCompileU.git
          cd hxCompileU
          haxe build.hxml
          cp export/haxeCompileU ../hxCompileU-bin
          cd ..
          ./hxCompileU-bin --prepare
          ./hxCompileU-bin --compile

      - name: Upload Artifact
        uses: actions/upload-artifact@main
        with:
          name: ${{ inputs.artifactName }}
          path: ${{ inputs.artifactPath }}
          if-no-files-found: error
